# llm_bringup

Launches the Gazebo world, Yolobot description, perception stack, OpenAI model node, and bounded robot tool server. Audio input and output are opt-in launch arguments.

See the repository root `README.md` for installation and launch examples.
