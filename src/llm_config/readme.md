# llm_config

Provides environment-backed runtime settings and strict OpenAI function-tool schemas for the `mobile`, `multi`, and `arm` robot profiles. Secrets are read from provider-standard environment or credential chains and are never written by this package.

See `.env.example` and the repository root `README.md` for available settings.
